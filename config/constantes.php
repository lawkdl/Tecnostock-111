<?php
/**
 * constantes.php
 * Constantes globales del proyecto.
 *
 * BASE_URL es la ruta del proyecto dentro del dominio (la carpeta que
 * creaste en htdocs). Se usa para que los enlaces y las redirecciones
 * funcionen igual sin importar si la página actual está en la raíz
 * (dashboard.php) o dentro de una subcarpeta (productos/listar.php).
 *
 * Si alguna vez renombras la carpeta del proyecto en htdocs, solo
 * tienes que actualizar este único valor.
 */
define('BASE_URL', '/tecnostock');
